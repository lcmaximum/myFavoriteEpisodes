import { useState } from 'react';

export default function EpisodeReviewForm({episode}) {

  /*  const [newReview, setNewReview] = useState({
        author: user,
        displayName: user.name,
        content: ""

    })*/
    return <div>
        <h2>the form to add a review would go here if it didn't crash the app</h2>
      {/*  <input type='text' value={newReview.content}  onChange={(e) =>
          setNewReview({ content: e.target.value })}/>*/}
</div>
}